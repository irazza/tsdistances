target/
# maturin's build output (`maturin build --out dist`, as CI and the README use)
dist/
.venv/
.vscode/
*.csv
# ...but the GPU crate's checked-in ACSF1 fixtures are .csv. They survive as
# already-tracked files, so the blanket ignore above is invisible until someone
# re-adds or moves them and git silently drops the change.
!crates/tsdistances_gpu/tests/ACSF1/*.csv
*.so
*.pyd
__pycache__/
compute_ee.py
.history/
requirements*
.pytest_cache/
.numba_cache/
.DS_Store
*.lock
*.svg
*.tex
*.npy